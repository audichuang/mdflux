{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "MDFlux",
  "version": "0.2.3",
  "identifier": "com.projektvisyo.mdflux",
  "build": {
    "beforeDevCommand": "npm run dev",
    "devUrl": "http://localhost:1420",
    "beforeBuildCommand": "npm run build",
    "frontendDist": "../build"
  },
  "app": {
    "windows": [
      {
        "title": "MDFlux",
        "width": 960,
        "height": 680,
        "minWidth": 680,
        "minHeight": 520,
        "resizable": true,
        "center": true
      }
    ],
    "security": {
      "csp": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: asset: http://asset.localhost; connect-src 'self' ipc: http://ipc.localhost"
    }
  },
  "bundle": {
    "active": true,
    "targets": ["nsis", "dmg"],
    "publisher": "ibrahimqureshae",
    "copyright": "© 2026 ibrahimqureshae",
    "category": "Productivity",
    "shortDescription": "Convert documents to clean Markdown — offline.",
    "longDescription": "MDFlux turns PDFs, Office files, and more into clean Markdown. Offline packages embed a private Python runtime and MarkItDown (Windows NSIS/portable; macOS arm64 DMG). Optional OCR/audio engines are not included in the basic offline build.",
    "macOS": {
      "minimumSystemVersion": "11.0"
    },
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "resources": [
      "resources/sidecar/*",
      "resources/sidecar/requirements.lock",
      "resources/sidecar/requirements-ocr.lock",
      "resources/sidecar/requirements-audio.lock",
      "resources/runtime/**/*"
    ],
    "windows": {
      "webviewInstallMode": {
        "type": "downloadBootstrapper"
      },
      "nsis": {
        "installMode": "currentUser",
        "displayLanguageSelector": false
      }
    }
  }
}
